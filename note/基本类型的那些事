  Java的数据类型分为两大类：基本类型和引用类型。
  基本类型有8种：byte，boolean，short，char，int，long，float，double
  引用类型有：数组、类、接口、泛型。
  
  基本类型的大小：
  byte（1个字节）-> short(2个字节) -> int (4个字节)->long（8个字节） -> float(4个字节) -> double（8个字节）
                   boolean
                   char
  基本类型之间的转换。
  1、自动转换
        由低精度向高精度可以自动转。内存的角度。因为Java虚拟机会给每个基本类型的变量开辟相对应的内存空间。大空间的类型可以容得下小空间的类型。
  2、强制转换
        高精度到低精度必须强制转换
